#include<stdio.h>
 int main(){
         
	 int num1,num2;
	 int sum,diff,mul;
	 
         printf("Enter two no.\n");
	 scanf("%d%d",&num1,&num2);

        
	 sum = num1+num2;
	 printf("sum of two no. is %d\n",sum);

	 diff = num1-num2;
	 printf("difference of two no. is %d\n",diff);

	 mul= num1*num2;
	 printf("multiplication of two no. is %d\n",mul); 

	 return 0;
 }
